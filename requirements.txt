# Standard pip packages for multi-animal-tracker
# Install with: uv pip install -v -r requirements.txt
#
# For GPU-accelerated version, use requirements-cuda12.txt or requirements-cuda13.txt instead

# PyTorch (CPU version)
# For GPU support, use requirements-cuda12.txt or requirements-cuda13.txt
torch
torchvision

# Computer vision & deep learning
timm
hnswlib
usearch
annoy
pupil-apriltags
ultralytics
platformdirs>=3.0

# Install this package in editable mode
-e .
