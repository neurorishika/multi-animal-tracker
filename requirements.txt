# CPU packages for hydra-suite
# Install with: uv pip install -v -r requirements.txt
#
# Base dependencies (numpy, scipy, PySide6, ultralytics, etc.) are declared
# in pyproject.toml and installed automatically via the editable install below.
# This file only adds packages that pyproject.toml cannot express (torch
# requires a custom index URL for GPU variants).

# PyTorch (CPU version from PyPI)
torch
torchvision

# Install this package in editable mode (pulls all pyproject.toml dependencies)
-e .
