# Minimal pip packages for multi-animal-tracker
# Install with: uv pip install -v -r requirements-minimal.txt
#
# Lightweight installation - only essential packages

# PyTorch (CPU version)
torch
torchvision

# YOLO models for object detection
ultralytics

# Install this package in editable mode
-e .
