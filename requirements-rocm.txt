# AMD ROCm GPU packages for hydra-suite
# Install with: uv pip install -v -r requirements-rocm.txt
#
# CRITICAL: System-level ROCm 6.0+ must be installed first.
# See ROCM_SETUP.md for prerequisites.
#
# Base dependencies are in pyproject.toml (installed via -e . below).
# This file adds torch and ROCm-specific packages only.

# PyTorch with ROCm support
# Uncomment ONE index URL matching your ROCm version:
--extra-index-url https://download.pytorch.org/whl/rocm6.2
# --extra-index-url https://download.pytorch.org/whl/rocm6.1
# --extra-index-url https://download.pytorch.org/whl/rocm6.0

torch
torchvision
torchaudio

# CuPy with ROCm support (GPU-accelerated background subtraction)
# Note: compilation can take 5-10 minutes on first install
cupy-rocm-6-0

# ONNX (CPU — no maintained ROCm provider)
onnx
onnxslim
onnxruntime

# Install this package in editable mode (pulls all pyproject.toml dependencies)
-e .
