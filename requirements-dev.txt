# Development & code-quality tools
# ─────────────────────────────────────────────────────────────────────────────
# Install after creating and activating a runtime conda environment:
#   make install-dev
#
# NOTE: the graphviz *binary* (dot) is not pip-installable and must be
# installed once via conda in your active environment:
#   conda install -c conda-forge graphviz

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0

# ── Formatting ───────────────────────────────────────────────────────────────
black>=24.0
isort>=5.13

# ── Linting ──────────────────────────────────────────────────────────────────
flake8>=7.0
pre-commit>=3.7

# ── Dead-code detection ───────────────────────────────────────────────────────
vulture>=2.11          # static dead-code scanner (used by make dead-code)
deadcode>=2.4          # automated dead-code removal (used by make dead-code-fix)

# ── Dependency analysis ───────────────────────────────────────────────────────
pydeps>=1.12           # visual dependency graph (requires graphviz dot binary)
pylint>=3.0            # pyreverse ships with pylint (text dependency map)

# ── Static type checking ──────────────────────────────────────────────────────
mypy>=1.9
